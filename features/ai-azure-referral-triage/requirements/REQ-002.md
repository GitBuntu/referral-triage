# Requirement REQ-002

---

## 🔗 SpecForge Chain Position
**This is artefact 2 of 7.**

## 🔗 SpecForge Chain
Context → **Requirements (1..N)** → Feature → Scenario → Test → Plan → Tasks  
              ↑ You are here

---

## EARS
**Event driven**: When a valid referral document has been submitted (Referral-Submitted event), the AI triage processor shall extract text from the document via OCR if needed, send the extracted text to an AI model, and the TriageRecord aggregate shall store the AI classification results ensuring TriageResult.Specialty is one of {cardiology, orthopaedics, neurology, dermatology, general_medicine}.

---

## Requirement Statement
When a valid referral document has been submitted and the Referral-Submitted event is emitted, the AI triage processor shall extract text from the ReferralDocument (using OCR if the document is scanned/image format), send the extracted text to an AI model, and receive back a structured classification result. The TriageRecord aggregate shall then accept the AI result, validate that Specialty is in the allowed set {cardiology, orthopaedics, neurology, dermatology, general_medicine}, and preserve the TriageRecord-SpecialtyInvariant. The TriageRecord shall then be immediately retrievable from ITriageRecordRepository by specialty.

---

## Invariants Preserved
- TriageRecord-SpecialtyInvariant

---

## Domain Events Triggered
- Referral-Triaged

---

## SpecForge Rules
- Must reference ≥1 invariant from the context OR ≥1 domain event from the context (or both).
- Must not introduce new invariants or events.
- All referenced invariants and events must exist in the Context — do not invent them.

---

## Next Step Directive
Create additional requirements (REQ-003, REQ-004, etc.) if needed, then create a feature specification.
