# Requirement REQ-003

---

## 🔗 SpecForge Chain Position
**This is artefact 2 of 7.**

## 🔗 SpecForge Chain
Context → **Requirements (1..N)** → Feature → Scenario → Test → Plan → Tasks  
              ↑ You are here

---

## EARS
**Event driven**: When an AI model generates triage classification results, the TriageRecord aggregate shall validate that Urgency is in {routine, soon, urgent}, that all required fields (patient_name, dob, symptoms, duration, red_flags) are present and non-empty in ExtractedFields, and that ClinicalSummary is non-empty and under 500 characters, preserving TriageRecord-UrgencyInvariant, TriageRecord-KeyFieldsInvariant, and TriageRecord-ClinicalSummaryInvariant.

---

## Requirement Statement
When an AI model generates triage classification results, the TriageRecord aggregate shall validate that Urgency is one of {routine, soon, urgent}, that ExtractedFields contains all required keys (patient_name, dob, symptoms, duration, red_flags) with non-empty string values, and that ClinicalSummary is a non-empty string with length less than 500 characters. If all validations pass, the TriageRecord shall emit Referral-Triaged event with all triage details in the payload, and the TriageRecord shall be immediately retrievable from ITriageRecordRepository by urgency and by ReferralId.

---

## Invariants Preserved
- TriageRecord-UrgencyInvariant
- TriageRecord-KeyFieldsInvariant
- TriageRecord-ClinicalSummaryInvariant

---

## Domain Events Triggered
- Referral-Triaged

---

## SpecForge Rules
- Must reference ≥1 invariant from the context OR ≥1 domain event from the context (or both).
- Must not introduce new invariants or events.
- All referenced invariants and events must exist in the Context — do not invent them.

---

## Next Step Directive
Create additional requirements if needed, then create a feature specification.
